name: macOS

on:
  push:
jobs:
  install-numpy:

    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]
        arch: [arm64]
        os: [macos-15]

    steps:
    - uses: astral-sh/setup-uv@v5
    - name: Run
      run: |
        uv python install 3.11
        uv pip install -v numpy
      env:
        MACOSX_DEPLOYMENT_TARGET: '11.0'